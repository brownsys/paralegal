// definition

{{#if node-cluster}}
{{{intro}}}
{{else}}
let {{{var}}}_base = {{{intro}}}
{{/if}}
{{#if filter}}
    .into_iter()
    .filter(|n| {
        let {{{inner_var}}} = *n;
        (|| { {{{filter}}} })().0
    })
{{/if}}
{{#if node-cluster}}
)
{{else}}
    .collect::<Vec<_>>();
    let {{{var}}} = IterCopied(&{{{var}}}_base);
{{/if}}
    ;