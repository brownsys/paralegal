use anyhow::Result;
use std::sync::Arc;

use paralegal_policy::{paralegal_spdg::{Identifier, traverse::EdgeSelection}, RootContext, Context, Marker,
assert_error};

macro_rules! marker {
($id:ident) => {
Marker::new_intern(stringify!($id))
};
}

pub fn check(ctx: Arc<RootContext>) -> Result<()> {
        ctx.named_policy(Identifier::new_intern("check"), |ctx| {
        {{policy}}
        Ok(())
        })
        }