{{intro}};
{{#if filter}}
    let mut {{var}}_nodes = {{inner_var}}_nodes.filter(|n| {
    let {{inner_var}} = *n;
    {{#if everywhere}}ctx.all_controllers().any(|(c_id, _)| { {{/if}}
    {{{filter}}}
    {{#if everywhere}}}){{/if}}
    });
{{/if}}
