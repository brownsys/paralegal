{
    {{#each src-intro}}
    {{this}}
    {{/each}}
    {{#each sink-intros}}
    {{this}}
    {{/each}}
    {{#each checkpoint-intros}}
    {{this}}
    {{/each}}

    ctx.always_happens_before(
        {{#each src}}{{this}}{{/each}},
        |checkpoint| {{#each checkpoint-names}}{{this}}.find(|n| *n == checkpoint).is_some(){{#unless @last}}{{#if checkpoint-or}} || {{else}} || {{/if}}{{/unless}}{{/each}},
        |sink| {{#each sink-names}}{{this}}.find(|n| *n == sink).is_some(){{#unless @last}}{{#if sink-or}} || {{else}} && {{/if}}{{/unless}}{{/each}},
    ).unwrap().holds()
}
