{{#if sink-node-cluster}}
  {{#if sink-for-each}}
    {{src}}.has_ctrl_influence_all(&{{sink}}, &ctx).is_none()
  {{else}}
    {{src}}.has_ctrl_influence(&{{sink}}, &ctx)
  {{/if}}
{{else}}
    {{src}}.has_ctrl_influence({{sink}}, &ctx)
{{/if}}