{
let {{sink}}_callsite = ctx.inputs_of(ctx.associated_call_site({{#if sink-definition}}*{{/if}}{{sink}}));
ctx.flows_to({{#if src-definition}}*{{/if}}{{src}}, &{{sink}}_callsite, EdgeSelection::Data)
}