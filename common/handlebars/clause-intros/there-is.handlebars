{{#unless refers-to-definition}}
    {{#if node-cluster}}
        {{intro}}) else { return false; };
    {{else}}
        let mut {{var}} = {{intro}};
    {{/if}}
{{/unless}}

{{#if node-cluster}}
{{body}}
{{else}}
    {{var}}
        .any(|{{var}}| {
            {{body}}
        })
{{/if}}