{{#unless refers-to-definition}}
    {{#if node-cluster}}
        {{intro}}) else { return true; };
    {{else}}
        let mut {{var}} = {{intro}};
    {{/if}}
{{/unless}}

{{#if node-cluster}}
{{body}}
{{else}}
    {{var}}
        .all(|{{var}}| {
            {{body}}
        })
{{/if}}