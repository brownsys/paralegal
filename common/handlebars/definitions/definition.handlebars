{{#if lifted}}
{{intro}}
{{else}}
let mut {{var}} = {{intro}})
{{/if}}
{{#if filter}}
    .filter(|n| {
        let {{inner_var}} = *n;
        {{{filter}}}
    })
{{/if}}
{{#if lifted}}
) else { return {{short-circuit}}; }
{{/if}}
;
