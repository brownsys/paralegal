Definitions:
1. "protected access" is each "db_access" marked db_read where:
	A. "db_access" is not marked db_user_read

Always:
1. For each "protected access":
    A. There is a "dc" marked instance_delete_check where:
        a. "dc" influences "db_write"
    and
    B. There is a "bc" marked instance_ban_check where:
        a. "bc" influences "db_write"