Scope: 
Everywhere

Definitions:
1. "stored_commit" is each "commit" marked commit where:
    A. There is a "store" marked store where:
        a. "commit" goes to "store"

2. "new_resource" is each "resource" marked new_resource where:
    A. There is a "stored_commit" where:
        a. "resource" goes to "stored_commit" 

Policy:
1. For each "stored_commit":
    A. There is a "auth_check" marked auth_check where:
        a. For each "new_resource":
            i) "new_resource" does not go to "auth_check"
        and
        b. "stored_commit" goes to "auth_check"
        and
        c. "auth_check" affects whether "stored_commit" happens