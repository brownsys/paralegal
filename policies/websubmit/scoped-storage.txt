Definitions:
1. "sensitive store" is each "store" marked store where:
	a. There is a "sensitive" marked sensitive where:
		i. "sensitive" goes to "store"

Always:
1. For each "sensitive store":
    a. There is a "scope" where:
        i. "scope" goes to the operation associated with "sensitive store"
        and
        ii. There is a "auth witness" where:
			1. "auth witness" goes to "scope"