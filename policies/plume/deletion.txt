In gdpr_deletes:
1. For each "stored data" type marked user_data:
    A. There is a "retrieval" that is a source of "stored data" where:
       a. There is a "deletes" marked deletes where:
	       i) "retrieval" goes to "deletes"