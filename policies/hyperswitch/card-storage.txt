Scope:
Everywhere

Policy:
1. For each "card" marked credit_card:
	A. For each "sink" marked sink:
		a. If "card" goes to "sink" then:
	    	i) There is a "consent" marked consent where:
				A) "consent" affects whether "sink" happens