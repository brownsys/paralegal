Definitions:
1. "stored_view" is each "pageview" marked pageview_data where:
	A. There is a "store" marked store where:
		a. "pageview" goes to "store"

Somewhere:
1. For each "stored_view":
	A. There is a "date_check" marked check_date where:
		a. There is a "deleter" marked deletes where:
			i) "date_check" affects whether "deleter" happens
		and
		b. "stored_view" goes to "date_check"