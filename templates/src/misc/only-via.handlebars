{
    {{src_intro}}
    {{sink_intro}}
    {{checkpoint_intro}}

    ctx.always_happens_before(
        {{src}}_nodes.collect::<Vec<_>>(), 
        |checkpoint| {{checkpoint}}_nodes.collect::<Vec<_>>().contains(&checkpoint), 
        |sink| {{sink}}_nodes.collect::<Vec<_>>().contains(&sink), 
    ).unwrap().holds()
}