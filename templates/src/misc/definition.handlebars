{{intro}};
let {{var}}_nodes = {{inner_var}}_nodes.filter(|n| {
    let {{inner_var}} = *n;
    {{#if everywhere}}self.cx.all_controllers().any(|(c_id, _)| {  
    {{{filter}}}
    }){{/if}}
}).collect::<Vec<_>>();
